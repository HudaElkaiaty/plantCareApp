import 'package:flutter/material.dart';

class ColorManager {
  // Brand Colors
  static const Color lightGreenColor = Color(0xff3DC854);
  static const Color greenColor = Color(0xff008615);
  static const Color whiteColor = Colors.white;
  static const Color scaffoldBackground = Color(0xFFF1F5F9);
  static final Color shadowColor = Colors.black.withValues(alpha: 0.03);

  // Chips 
  static const Color chipBackground = Color(0xFFE8F5E9); 
  static const Color chipBorder = Color(0xFFA5D6A7); 
  static const Color chipText = Color(0xFF2E7D32); 
  static const Color stepNumberText = Color(0xFF388E3C);

  // (Icons)
  static const Color waterBlue = Colors.blue; 
  static const Color soilGreen = Colors.green; 
  static const Color divisionRed = Colors.redAccent; 
  static const Color leafTeal = Colors.teal;

  //(Typography)
  static const Color textDarkHeader = Color(0xFF0F172A); 
  static const Color textDarkBody = Color(0xFF1E293B);   
  static const Color textStepBody = Color(0xFF334155);  
  static const Color textMuted = Color(0xFF757575);      

  //(Dividers)
  static const Color dividerColor = Color(0xFFE0E0E0);

  // care tips
static const Color borderGray = Color(0xFFE2E8F0); 
static const Color bulletGreen = Color(0xFF388E3C); 
static const Color textLightSlate = Color(0xFF475569);

static const Color errorRed = Colors.red; 

static const Color textCharcoal = Color(0xFF2D3748); 

static const Color lightSlateBackground = Color(0xFFF8FAFC);

static final Color detailShadowColor = Colors.black.withValues(alpha: 0.05);
}